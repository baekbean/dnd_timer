// Shared `d`-attribute substrings for asserting which SpeakerIcon branch
// rendered (cone always present, plus either the volume-2 wave-arc paths or
// the volume-x X-mark paths) — kept in one place so a future re-trace of the
// glyph only requires editing here instead of every consuming test file.
export const CONE_PATH = 'M10.4332 0.0987061'
export const WAVE_ARC_PATH = '14.3999 4.20006'
export const X_MARK_PATH = '21.7071 4.29289'
